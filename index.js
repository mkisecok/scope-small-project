// Aufgabe 1

const student = {
    firstName: 'John',
    lastName: 'Smith',
    class:12
};
let result= `${student.firstName} ${student.lastName} is a student in class ${student.class}`;
console.log(result);